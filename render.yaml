services:
  - type: web
    name: gamepoch-content-copilot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /api/demo/status
    envVars:
      - key: TZ
        value: Asia/Shanghai
